import { type NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
    return (
        <>
            <Head>
                <title>One Zero</title>
                <meta name="One Zero" />
            </Head>
            <main>hello world!</main>
        </>
    );
};

export default Home;
